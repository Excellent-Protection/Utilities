using System;
using System.Collections.Generic;
using System.Data.Entity.ModelConfiguration;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Utilities.DataAccess.Configuration.Identity
{
   //public class UserProjectConfiguration : EntityTypeConfiguration<UserProject>
   // {
   //     public UserProjectConfiguration()
   //     {
   //         HasKey(a => a.Id);
   //         HasOptional(a => a.User).WithMany().HasForeignKey(x => x.UserId);
   //         HasOptional(a => a.Role).WithMany().HasForeignKey(x => x.RoleId);
   //     }
   // }
}
